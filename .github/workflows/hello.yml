name: Hello CI

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from CI!"
      - run: git rev-parse --short HEAD
      - run: date
